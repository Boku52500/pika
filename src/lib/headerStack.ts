/** Search row sits above the category nav inside the sticky header stacking context. */
export const HEADER_SEARCH_STACK_CLASS = "relative z-20 overflow-visible";
export const CATEGORY_NAV_STACK_CLASS = "relative z-10";
